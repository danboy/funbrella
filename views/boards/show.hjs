{{#board.widgets}}
  <link rel="stylesheet" href="/widgets/{{name}}/css/style.css" />
{{/board.widgets}}
{{#error}}
  <p>{{error}}</p>
{{/error}}
{{#board}}
  <div id="board">
      <div class="message-board" id="message-board"></div>
    {{#widgets}}
      <div class="widget {{name}}" id="widget-{{name}}">
        loading...
      </div>
    {{/widgets}}
  </div>
{{/board}}
{{^board}}
  <p class="error">No board found with id {{socket}}.</p>
{{/board}}
